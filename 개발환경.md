# 강화학회 모각코 개발환경 세팅 (시즌 1)

## 1. VSCode 설치

https://code.visualstudio.com 에서 다운로드

## 1_1. 참고 (선택)

**VSCode + Continue 확장**: VSCode Extensions에서 "Continue" 검색 후 설치. 필수는 아닙니다.(로컬 LLM 및 사내 보안이슈 환경에서 꽤 좋은 조합입니다.)

## 2. Python 환경

Python 3.10 이상 필요 (3.11.9 권장)

```bash
pip install gymnasium torch matplotlib numpy tensorboard pygame moviepy
```

설치 확인:

```bash
python -c "import gymnasium; import torch; print('OK')"
```

OK가 출력되면 준비 완료

## 3. 실행

### REINFORCE

```bash
python REINFORCE.py
```

터미널에 avg score가 올라가는 게 보이고, 끝나면 reinforce_result.png 파일이 생성됩니다.

### Actor-Critic

```bash
python actor_critic.py
```

REINFORCE와 비교하여 학습 안정성의 차이를 확인해보세요.

### PPO

```bash
python ppo.py
```

Actor-Critic과 비교하여 학습 속도와 안정성의 차이를 확인해보세요.

### DQN

```bash
python dqn.py
```

Policy Gradient 계열(REINFORCE/AC/PPO)과 다른 Value-based 접근의 학습 패턴을 확인해보세요.

### DDPG

```bash
python ddpg.py
```

환경이 CartPole에서 **Pendulum**으로 바뀌고, action이 처음으로 **continuous(-2.0~2.0)**가 됩니다.
Pendulum의 reward는 음수이며, -200에 가까울수록 잘 학습된 것입니다.

### TD3

```bash
python td3.py
```

DDPG와 같은 Pendulum 환경에서 동작하지만, 세 가지 개선(Twin Critic, Delayed Policy Update, Target Policy Smoothing)이 추가됩니다.

### SAC

```bash
python sac.py
```

탐색을 noise로 수동 조절하는 대신 **entropy로 자동 조절**합니다. alpha 값이 자동으로 변하는 과정을 확인해보세요.

### TD3+BC (Offline RL)

```bash
python td3_bc.py
```

여기서부터 **Offline RL**입니다. **학습 중 환경과 상호작용하지 않습니다.**

실행은 세 단계로 진행됩니다:
1. behavior policy 학습 (SAC를 돌려 좋은 정책을 만듦, `sac_behavior.pt`에 캐싱)
2. 그 정책으로 Pendulum을 돌려 데이터셋 수집
3. 고정된 데이터셋만으로 TD3+BC 학습 (env.step() 없음)

TD3에 BC 항 `(π(s) - a)²`를 더해 데이터 행동에서 벗어나지 못하게 제약합니다. (정책 제약)

### CQL (Offline RL)

```bash
python cql.py
```

TD3+BC가 **정책**을 제약했다면, CQL은 **가치(Q함수)**를 제약합니다.
TD3+BC와 동일한 3단계(SAC behavior policy로 데이터 수집)로 진행되며, 같은 데이터셋을 써서 두 방식을 비교할 수 있습니다.

SAC의 Q-loss에 CQL 정규화 항을 더한 것이 핵심입니다. `cql_alpha`가 보수성의 강도를 결정합니다.

### IQL (Offline RL)

```bash
python iql.py
```

오프라인 RL의 완성형입니다. TD3+BC / CQL과 달리 **OOD 행동을 아예 평가하지 않습니다.**
TD3+BC / CQL과 동일한 3단계(SAC behavior policy로 데이터 수집)로 진행되어, 세 방법을 같은 데이터셋에서 비교할 수 있습니다.

세 단계로 학습합니다:
1. Value 학습 (Expectile regression) — 데이터 안 행동 중 좋은 쪽을 추정
2. Q 학습 — 다음 상태 V(s')만 사용, OOD 평가 차단
3. 정책 추출 (AWR) — advantage 큰 행동을 더 강하게 모방

`expectile`(0.5=평균, 1=낙관)과 `beta`(AWR 온도)가 튜닝 포인트입니다.

**Offline RL 알고리즘 비교:**

| | TD3+BC | CQL | IQL |
|---|---|---|---|
| 베이스 | TD3 | SAC | V + Q + AWR |
| OOD 처리 | 정책 제약 | 가치 누르기 | 평가 회피 |
| 추가 네트워크 | - | - | V network |
| 핵심 | `(π(s)-a)²` | `L_CQL` (logsumexp) | Expectile + AWR |
| 튜닝 | alpha_bc | cql_alpha | expectile, beta |

## 4. TensorBoard로 결과 보기

### 알고리즘별

```bash
tensorboard --logdir=~/tb_logs/REINFORCE
tensorboard --logdir=~/tb_logs/ActorCritic
tensorboard --logdir=~/tb_logs/PPO
tensorboard --logdir=~/tb_logs/DQN
tensorboard --logdir=~/tb_logs/DDPG
tensorboard --logdir=~/tb_logs/TD3
tensorboard --logdir=~/tb_logs/SAC
tensorboard --logdir=~/tb_logs/TD3BC
tensorboard --logdir=~/tb_logs/CQL
tensorboard --logdir=~/tb_logs/IQL
```

### 모든 알고리즘 동시 비교

```bash
tensorboard --logdir=~/tb_logs
```

같은 Pendulum 환경의 알고리즘 비교에 특히 유용합니다.
- Online: DDPG vs TD3 vs SAC (탐색 방식의 진화)
- Offline: TD3+BC vs CQL vs IQL (정책 제약 vs 가치 누르기 vs OOD 회피)

브라우저에서 http://localhost:6006 접속

- **SCALARS 탭**: 학습 곡선. SAC/CQL은 `alpha`, Offline은 `score/eval` 그래프 확인
- **IMAGES 탭**: 환경 영상 (CartPole 또는 Pendulum)

Online RL은 episode 단위, Offline RL은 training step 단위로 기록됩니다.

**환경별 reward 해석:**
- CartPole: 매 step +1, 최대 500 (높을수록 좋음)
- Pendulum: 매 step 음수 페널티, 최적 약 -200 (0에 가까울수록 좋음)

## 시즌 1 마무리

REINFORCE부터 IQL까지, 10주간 온라인 RL과 오프라인 RL의 핵심 알고리즘을 코드로 직접 돌려봤습니다.
시즌 2에서는 gym 환경을 직접 만들고 실제 산업 문제에 RL을 적용하는, 응용된 버전의 모각코로 찾아뵙겠습니다.
