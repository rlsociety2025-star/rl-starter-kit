# 강화학회 모각코 개발환경 세팅

## 1. VSCode 설치

https://code.visualstudio.com 에서 다운로드

## 1_1. 참고 (선택)

**VSCode + Continue 확장**: VSCode Extensions에서 "Continue" 검색 후 설치. 필수는 아닙니다.(로컬 LLM 및 사내 보안이슈 환경에서 꽤 좋은 조합입니다.)

## 2. Python 환경

Python 3.10 이상 필요 (3.11.9 권장)

```bash
pip install gymnasium torch matplotlib numpy tensorboard pygame moviepy
```

설치 확인:

```bash
python -c "import gymnasium; import torch; print('OK')"
```

OK가 출력되면 준비 완료

## 3. 실행

### REINFORCE

```bash
python REINFORCE.py
```

터미널에 avg score가 올라가는 게 보이고, 끝나면 reinforce_result.png 파일이 생성됩니다.

### Actor-Critic

```bash
python actor_critic.py
```

터미널에 avg score가 출력됩니다. REINFORCE와 비교하여 학습 안정성의 차이를 확인해보세요.

### PPO

```bash
python ppo.py
```

터미널에 avg score가 출력됩니다. Actor-Critic과 비교하여 학습 속도와 안정성의 차이를 확인해보세요.

### DQN

```bash
python dqn.py
```

터미널에 avg score, buffer 크기, epsilon이 출력됩니다. Policy Gradient 계열(REINFORCE/Actor-Critic/PPO)과 달리 Value-based 방식이며, Replay Buffer와 Target Network를 사용합니다.

**REINFORCE vs Actor-Critic vs PPO vs DQN 차이점:**
| | REINFORCE | Actor-Critic | PPO | DQN |
|---|---|---|---|---|
| 계열 | Policy Gradient | Policy Gradient | Policy Gradient | Value-based |
| 네트워크 | Policy만 사용 | Policy(Actor) + Value(Critic) | Policy(Actor) + Value(Critic) | Q-network + Target Q |
| 학습 방식 | On-policy | On-policy | On-policy | Off-policy (Replay Buffer) |
| 탐험 | 확률적 정책 | 확률적 정책 | 확률적 정책 | ε-greedy |
| 업데이트 | 에피소드 끝난 후 | n-step(10) rollout마다 | n-step(20) rollout마다 | 매 에피소드 후 batch 샘플링 |
| 데이터 재사용 | 1회 | 1회 | K회 (K_epoch=3) | Buffer 내 반복 사용 |
| 분산 | 높음 (Monte Carlo) | 낮음 (TD 기반 baseline) | 더 낮음 (GAE + clipping) | 낮음 (TD + Target net) |
| 핵심 | log-prob × return | TD error as advantage | GAE + clipped surrogate | Replay Buffer + Target Net |

## 4. TensorBoard로 CartPole 영상 보기

### REINFORCE

```bash
tensorboard --logdir=~/tb_logs/REINFORCE
```

### Actor-Critic

```bash
tensorboard --logdir=~/tb_logs/ActorCritic
```

### PPO

```bash
tensorboard --logdir=~/tb_logs/PPO
```

### DQN

```bash
tensorboard --logdir=~/tb_logs/DQN
```

### 네 알고리즘 동시 비교

```bash
tensorboard --logdir=~/tb_logs
```

브라우저에서 http://localhost:6006 접속

- **SCALARS 탭**: 학습 곡선 (score가 올라가는 과정)
- **IMAGES 탭**: CartPole 영상 (막대가 균형 잡는 모습)

300에피소드(video_interval로 변경)마다 영상이 녹화됩니다. episode 0 (랜덤) → episode 2700 (학습 후) 비교해보세요.
