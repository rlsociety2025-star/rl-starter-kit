# 강화학회 모각코 개발환경 세팅

## 1. VSCode 설치

https://code.visualstudio.com 에서 다운로드

## 1_1. 참고 (선택)

**VSCode + Continue 확장**: VSCode Extensions에서 "Continue" 검색 후 설치. 필수는 아닙니다.(로컬 LLM 및 사내 보안이슈 환경에서 꽤 좋은 조합입니다.)

## 2. Python 환경

Python 3.10 이상 필요 (3.11.9 권장)

```bash
pip install gymnasium torch matplotlib numpy tensorboard pygame moviepy
```

설치 확인:

```bash
python -c "import gymnasium; import torch; print('OK')"
```

OK가 출력되면 준비 완료

## 3. 실행

### REINFORCE

```bash
python REINFORCE.py
```

터미널에 avg score가 올라가는 게 보이고, 끝나면 reinforce_result.png 파일이 생성됩니다.

### Actor-Critic

```bash
python actor_critic.py
```

터미널에 avg score가 출력됩니다. REINFORCE와 비교하여 학습 안정성의 차이를 확인해보세요.

### PPO

```bash
python ppo.py
```

터미널에 avg score가 출력됩니다. Actor-Critic과 비교하여 학습 속도와 안정성의 차이를 확인해보세요.

### DQN

```bash
python dqn.py
```

터미널에 avg score, buffer size, epsilon이 출력됩니다.
Policy Gradient 계열(REINFORCE/AC/PPO)과 다른 Value-based 접근의 학습 패턴을 확인해보세요.

### DDPG

```bash
python ddpg.py
```

터미널에 avg score, buffer size가 출력됩니다.
환경이 CartPole에서 **Pendulum**으로 바뀌고, action이 처음으로 **continuous(-2.0~2.0)**가 됩니다.
Pendulum의 reward는 음수이며, -200에 가까울수록 잘 학습된 것입니다.

### TD3

```bash
python td3.py
```

터미널에 avg score, buffer size가 출력됩니다.
DDPG와 같은 Pendulum 환경에서 동작하지만, 세 가지 개선(Twin Critic, Delayed Policy Update, Target Policy Smoothing)이 추가되어 더 안정적인 학습을 보입니다.
DDPG 학습 곡선과 겹쳐서 비교해보세요.

### SAC

```bash
python sac.py
```

터미널에 avg score, buffer size, alpha가 출력됩니다.
DDPG/TD3와 같은 Pendulum 환경이지만, 탐색을 noise로 수동 조절하는 대신 **entropy로 자동 조절**합니다.
alpha 값이 학습에 따라 자동으로 변하는 과정을 확인해보세요. DDPG, TD3와 겹쳐서 비교하면 좋습니다.

### TD3+BC (Offline RL)

```bash
python td3_bc.py
```

여기서부터 **Offline RL**입니다. Week 1~7과 가장 크게 다른 점은 **학습 중 환경과 상호작용하지 않는다**는 것입니다.

실행은 세 단계로 진행됩니다:
1. behavior policy 학습 (SAC를 돌려 좋은 정책을 만듦, `sac_behavior.pt`에 캐싱되어 재실행 시 생략)
2. 그 정책으로 Pendulum을 돌려 데이터셋 수집 (환경 상호작용은 여기까지만)
3. 고정된 데이터셋만으로 TD3+BC 학습 (학습 루프에 env.step()이 없음)

TD3에 BC(Behavior Cloning) 항을 더한 것이 핵심입니다.
정책 손실에 `(π(s) - a)²` 항이 추가되어, 데이터에 있던 행동에서 너무 벗어나지 못하게 제약합니다. (정책 제약 방식)
데이터 품질이 성능의 상한을 결정합니다 (SAC 데이터 -150, 무작위 데이터 -1,200).

### CQL (Offline RL)

```bash
python cql.py
```

TD3+BC와 같은 Offline RL이지만, 문제를 푸는 위치가 다릅니다.
TD3+BC가 **정책**을 제약했다면, CQL은 **가치(Q함수)**를 제약합니다.

실행은 TD3+BC와 동일하게 세 단계로 진행됩니다:
1. behavior policy 학습 (SAC를 돌려 좋은 정책을 만듦, `sac_behavior.pt`에 캐싱)
2. 그 SAC 정책으로 Pendulum을 돌려 데이터셋 수집 (환경 상호작용은 여기까지)
3. 고정된 데이터셋만으로 CQL 학습 (환경 상호작용 없음)

지난주 TD3+BC와 같은 SAC 데이터셋을 쓰기 때문에, 정책 제약과 가치 제약을 같은 조건에서 비교할 수 있습니다.

베이스는 SAC이고, SAC의 Q-loss에 CQL 정규화 항을 더한 것이 핵심입니다.
"데이터에 없는 행동의 Q값은 누르고(logsumexp 항), 데이터에 있는 행동의 Q값은 올리는" 항이 추가됩니다.
그 결과 Q함수가 보수적으로 변해, 정책이 OOD 행동으로 빠지지 않습니다.

`cql_alpha`(코드 상단)가 보수성의 강도를 결정합니다.
너무 크면 학습이 안 되고, 너무 작으면 OOD 과대평가를 못 막습니다.
강의노트에서 "alpha 튜닝이 까다롭다"고 짚은 부분이니, 값을 바꿔가며 실험해보세요.

**Offline RL 알고리즘 비교:**

| | TD3+BC | CQL |
|---|---|---|
| 베이스 | TD3 | SAC |
| 정책 | 결정적 | 확률적 |
| 해결 위치 | 정책 제약 (Actor) | 가치 제약 (Critic) |
| 핵심 | Actor 손실에 BC 항 `(π(s)-a)²` | Q 손실에 CQL 정규화 `L_CQL` |
| 튜닝 포인트 | alpha_bc (BC 가중치) | cql_alpha (보수성 강도) |

## 4. TensorBoard로 결과 보기

### 알고리즘별

```bash
tensorboard --logdir=~/tb_logs/REINFORCE
tensorboard --logdir=~/tb_logs/ActorCritic
tensorboard --logdir=~/tb_logs/PPO
tensorboard --logdir=~/tb_logs/DQN
tensorboard --logdir=~/tb_logs/DDPG
tensorboard --logdir=~/tb_logs/TD3
tensorboard --logdir=~/tb_logs/SAC
tensorboard --logdir=~/tb_logs/TD3BC
tensorboard --logdir=~/tb_logs/CQL
```

### 모든 알고리즘 동시 비교

```bash
tensorboard --logdir=~/tb_logs
```

DDPG vs TD3 vs SAC처럼 같은 환경(Pendulum)의 알고리즘 비교에 특히 유용합니다.
Offline RL은 TD3+BC vs CQL을 겹쳐 보면 정책 제약 vs 가치 제약의 차이를 확인할 수 있습니다.

브라우저에서 http://localhost:6006 접속

- **SCALARS 탭**: 학습 곡선 (score가 변하는 과정). SAC/CQL은 `alpha`, Offline(TD3+BC/CQL)은 `score/eval` 그래프 확인
- **IMAGES 탭**: 환경 영상 (CartPole 또는 Pendulum)

Online RL은 episode 단위, Offline RL(TD3+BC/CQL)은 training step 단위로 기록됩니다.

**환경별 reward 해석:**
- CartPole: 매 step +1, 최대 500 (높을수록 좋음)
- Pendulum: 매 step 음수 페널티, 최적 약 -200 (0에 가까울수록 좋음)
